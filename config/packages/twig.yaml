twig:
    default_path: '%kernel.project_dir%/templates'

    globals:
        siteKey: '%env(SITEKEY)%'
        keycloakAdminUrl: '%env(KEYCLOAK_ADMIN_URL)%'

when@test:
    twig:
        strict_variables: true
