{% extends 'base.html.twig' %}

{% block content %}
    <div class="col-xs-12">
        <h2 class="h1">
            Edit Roles
        </h2>
    </div>
    <div id="roles-edit-app">
        <roles-edit-app
            init-urls="{{ urls }}"
            init-data="{{ roles }}"
        />
    </div>
{% endblock %}

{% block javascript %}
    {{ vite_entry_script_tags('main') }}
    {{ vite_entry_script_tags('rolesedit') }}
{% endblock %}
