{% if not item.getPublic() %}
    {% if is_granted('ROLE_VIEW_INTERNAL') %}
        <span class="bg-warning">
            {% block renderDetail %}
                <a href="{{ path(path, {'id': item.getId()}) }}" {% if class is defined and class is not empty %}class="{{class}}"{% endif %}>{{ display }}</a>
            {% endblock %}
        </span>
    {% endif %}
{% else %}
    {{ block('renderDetail') }}
{% endif %}
