{% extends 'base.html.twig' %}

{% block content %}
    <div class="col-xs-12">
        <h2 class="h1">
            Edit genres
        </h2>
    </div>
    <div id="genres-edit-app">
        <genres-edit-app
            init-urls="{{ urls }}"
            init-data="{{ genres }}"
        />
    </div>
{% endblock %}

{% block javascript %}
    {{ vite_entry_script_tags('main') }}
    {{ vite_entry_script_tags('genresedit') }}
{% endblock %}
