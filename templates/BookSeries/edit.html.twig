{% extends 'base.html.twig' %}

{% block content %}
    <div class="col-xs-12">
        <h2 class="h1">
            Edit Book series
        </h2>
    </div>
    <div id="book-seriess-edit-app">
        <book-seriess-edit-app
            init-urls="{{ urls }}"
            init-data="{{ book_seriess }}"
        />
    </div>
{% endblock %}

{% block javascript %}
    {{ vite_entry_script_tags('main') }}
    {{ vite_entry_script_tags('bookseriessedit') }}
{% endblock %}
